{{- $limit := .Site.Params.localSearch.limit | default 1000 -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<search>
  {{- range where .Site.RegularPages "Kind" "page" }}
  {{- $content := .Plain }}
  {{- if gt .WordCount $limit }}
    {{- $content = .Plain | truncate $limit }}
  {{- end }}
  <entry>
    <title>{{ .Title | htmlEscape }}</title>
    <url>{{ .RelPermalink | relLangURL | htmlEscape }}</url>
    <categories>{{ range .Params.categories }}<category>{{ . | htmlEscape }}</category>{{ end }}</categories>
    <tags>{{ range .Params.tags }}<tag>{{ . | htmlEscape }}</tag>{{ end }}</tags>
    <content type="text">{{ $content | htmlEscape }}</content>
  </entry>
  {{- end }}
</search>
